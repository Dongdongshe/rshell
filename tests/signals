# test cd
pwd
cd .             # cd a relative path
pwd
cd src/rshell.cpp #cd a file
cd src        #cd a directory
pwd
cd                #cd non 
pwd
cd /directory/not/exist   # cd a non-exist directory
cd /home/                  #cd a absolute path
cd /root/                  # cd a dir without permission

#test commands from different directories
#from rshell/bin/ls
ls
# from /usr/bin:
md5sum --help >/dev/null
# from /usr/sbin:
ifconfig >/dev/null
# from /sbin:
mkfs --help >/dev/null
nonexistcommand   #non-exist command

# test Ctrl+C
cat

# test Ctrl+C again
top


#case for some extra credit:
#case for fg
cat

ps
fg

#case for bg
ping 8.8.8.8

bg

# test multiples 

